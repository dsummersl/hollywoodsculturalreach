require = window.require
Spine = require 'spine'
Spine.Model.Ajax = {}
Overview = require('models/overview')

describe 'Overview', ->
  beforeEach ->
    Overview.
  
  it 'can compute totalHollyWoodRatio', ->
    Overview.
    
