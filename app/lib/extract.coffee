require('lib/setup')

extractDomesticMovies = (datafile,year)->
  # get the domestic tickets.
 
extractCountrySummary = (datafile,year)->
  # try to get the year, and then mush it out.

module.exports =
